alias top='htop'
alias g='git status'
alias uu='yes | yay --noconfirm -Syyu --needed --devel'
alias lsorphans='sudo pacman -Qdt'
alias rmorphans='sudo pacman -Rns $(lsorphans| cut -d " " -f 1)'

# List aliases
alias l='ls -1'
alias ll='ls -hol'
alias la='ls -aA'
alias df='df -h'
alias sortmirrors='sudo reflector --verbose -c Germany -c France -c Italy -l 200 -p http --sort rate --save /etc/pacman.d/mirrorlist; sudo pacman -Syyu'
alias gl='git log --oneline'
alias less='bat --decorations never'
alias vim='nvim'

# Utils
alias list_npm_scripts='jq .scripts package.json'
alias task='go-task'
alias docker-stop-all='docker stop $(docker ps -aq)'
alias docker-rm-all='docker rm $(docker ps -aq)'
alias or='cd ~/repos/$(ls ~/repos | fzf) && git fetch -Ppaf'
alias ow='cd $(open-worktree.sh)'
alias git-add-remote-refs='git config remote.origin.fetch "+refs/heads/*:refs/remotes/origin/*"'
alias hr='$(history | cut -c 8- | fzf)'
